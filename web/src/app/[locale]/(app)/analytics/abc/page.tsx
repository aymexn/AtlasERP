import ABCClient from './abc-client';

export const metadata = {
  title: 'Classification ABC | AtlasERP',
  description: 'Analyse de la valeur du stock et classification ABC',
};

export default function ABCPage() {
  return <ABCClient />;
}
